{
  "name": "@harleys-website/preload-pages",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "yarn build:esbuild && yarn build:tsc",
    "build:esbuild": "node ../../esbuild.config.mjs",
    "build:tsc": "tsc -p tsconfig.build.json"
  },
  "author": "Harley Lang",
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.14.8"
  },
  "devDependencies": {
    "@types/node": "^17.0.4",
    "typescript": "^4.5.4"
  }
}
